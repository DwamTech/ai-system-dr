# AI-DOCUMENT-TOOLS-IMPLEMENTATION-021

**التاريخ:** 2026-09-06  
**الفرع:** `conference-v1`  
**الحالة:** **READY_FOR_HUMAN_REVIEW**  
**الصورة المختبرة:** `sha256:ceeecfe78cad870c77e2c96c6c285eb507acfc16061db2221eac1f46be248837`

## النتيجة

أُغلقت العيوب الوظيفية الأساسية الواردة في المراجعتين 016 و019. الأدوات الست غير المحادثة تعمل الآن كوظائف خلفية دائمة ومملوكة لمساحة المستخدم، بينما يظل الأرشيف العام ظاهرًا للجميع. لا تنفذ Streamlit معالجة المستند أو استدعاء النموذج في وضع المنصة، ولا يُحفظ نجاح فارغ أو ناتج لا يطابق عقد الأداة.

## ما نُفذ

- دورة Job مشروطة بملكية lease، مع إنهاء ذري، إلغاء نهائي، حد محاولات، واستعادة دورية للوظائف التي فقدت عاملها.
- مصالحة النتيجة المكتوبة قبل تعطل العامل، ونقاط حفظ خاصة وذرية لكل جزء مكتمل من استدعاءات الملخص والكيانات والترجمة والتحليل والخريطة وتحليل البحث.
- عقود Pydantic صارمة للطلبات والنتائج، تشمل UUIDs، خيارات منفصلة لكل أداة، نطاق صفحات الترجمة، اكتمال التغطية، وعدم قبول خيارات إضافية.
- artifact كامل ومرتب لكل إصدار، مع checksum، أرقام صفحات PDF الأصلية، الصفحات الفارغة، `has_text`، وعدّ صفحات النص.
- OCR عربي/إنجليزي للملفات المصورة، وOCR انتقائي للصفحات المصورة داخل PDF مختلط دون تغيير رقم الصفحة.
- map/reduce بمدخلات محدودة الحجم بدل إرسال المستند كاملًا في طلب واحد، مع إصلاح JSON واحد ونتيجة خطأ واضحة عند فشل التحقق.
- ملخص منظم مع نقاط ومراجع صفحات مقتبسة من النص الأصلي، وكيانات مع النوع والثقة والتكرار والصفحات.
- ترجمة full/page/range مع الأسلوب والمحافظة على التنسيق ومسرد موحد وصفحات قابلة للاستئناف.
- تحليل فعلي للمقاييس والموضوعات والمصطلحات والمقارنة بين المستندات مع أدلة الصفحات؛ أزيلت نتيجة المقارنة المؤقتة.
- خريطة ذهنية متصلة وبلا دورات، بعقد فريدة ومراجع صفحات، مع تقليم حتمي إلى العدد المطلوب وإصلاح دلالي واحد إذا لزم.
- بحث SearXNG بجلسة HTTP وretry محدود واكتشاف المحركات وcircuit breaker وDOI/URL canonical وترتيب وdeduplication.
- تحليل نتائج البحث لا يقبل إلا نتيجة بحث خاصة ومكتملة، ويعيد فقط أرقام مصادر موجودة ومتحققًا منها.
- واجهة موحدة لاستعادة أحدث وظيفة بعد refresh، منع الضغط المكرر أثناء العمل أو انقطاع API، الإلغاء، إعادة المحاولة، بدء نتيجة جديدة، والتنزيل من الخادم.
- عرض منظم للملخص والكيانات والترجمة والتحليل والخريطة والبحث وتحليل البحث دون JSON خام في المسار الأساسي.
- طابور وعامل مستقلان للبحث والكيانات السريعة حتى لا تنتظر خلف الترجمة أو الخريطة، مع بوابة مركزية لاستدعاءات النموذج ومهلة انتظار 180 ثانية.
- readiness حقيقي يفحص PostgreSQL وRedis والتخزين والطوابير الأربعة، وmetrics خاصة تعرض الانتظار والتنفيذ والحالات والأخطاء والمحاولات لكل أداة.
- migration ذات إصدار مسجل في `schema_migrations`، وbackfill قابل للاستئناف ويدعم `--dry-run`.
- نسخ احتياطي لـPostgreSQL وملفات المستندات والنتائج والمرفوعات، مع سكربت استعادة يتطلب `-ConfirmRestore` صراحة.

## أدلة القبول

| البوابة | النتيجة |
|---|---|
| compile + unit + contracts | PASS — 27 اختبارًا؛ تحذيرا deprecation معروفان فقط |
| عقود الطلب والنتيجة | PASS — الخطأ يُرفض قبل Outbox أو ينتهي `invalid_model_output` بلا نجاح شكلي |
| الإلغاء وidempotency | PASS — `cancelled` نهائية، والطلب المكرر يعيد Job نفسه |
| ترجمة صفحة خارج النطاق | PASS — HTTP 422 قبل إنشاء Job |
| الأدوات السبع الحية | PASS — summary, entities, translation, analysis, mindmap, web search, web analysis والتنزيل |
| الخريطة بعد الإصلاح | PASS — 64.41 ثانية، رسم متصل صالح |
| البحث الأكاديمي | PASS — 6.63 ثوانٍ |
| تحليل البحث بمصادر متحققة | PASS — 8.15 ثانية |
| 10 مستخدمين، حمل حتمي، جولتان بعد النشر النهائي | PASS — 20/20؛ p95 = 3.86s ثم 3.53s |
| 10 مستخدمين، حمل مختلط، الجولة 1 | PASS — 10/10؛ p50 = 52.18s، p95 = 118.91s |
| 10 مستخدمين، حمل مختلط، الجولة 2 | PASS — 10/10؛ p50 = 26.72s، p95 = 93.50s |
| 10 مستخدمين، حمل مختلط على الصورة النهائية | PASS — 10/10؛ p50 = 31.38s، p95 = 139.53s؛ محادثتان وكل الأدوات الست غير المحادثة |
| عزل النتائج | PASS — مستخدم آخر يحصل على 404، والأرشيف نفسه ظاهر للمساحات كلها |
| Browser 1440/768/390/375 | PASS — 7 tabs، بلا overflow أفقي، page errors أو console errors |
| تمرير الموبايل 390/375 | PASS — سحب لمسي حقيقي من داخل مساحة المستندات حرّك `stMain` من 0 إلى 345px، وتمرير العجلة إلى 420px؛ الاختبار كان يفشل قبل الإصلاح |
| عرض الخريطة الذهنية | PASS — حزمة Graphviz 0.21 موجودة في صورة Streamlit، مع PyTorch 2.14 CPU وSentence Transformers 6.0.1 |
| readiness | PASS — database/broker/storage و`generation,indexing,tools,tools-fast` |
| image parity | PASS — API وStreamlit وكل العمال تعمل من digest واحد |
| المهام العالقة بعد الاختبارات | PASS — صفر active jobs |
| استقرار Docker المحلي | PASS — نُقل قرص بيانات Docker من قرص النظام الممتلئ إلى `F:\DockerData\wsl\disk`، ثم أُعيد البناء من الصفر واختُبرت الصورة |
| النسخ الاحتياطي | PASS — `postgres.dump` قابل للفهرسة وarchive الملفات قابل للقراءة |

توزيع الجولة المختلطة كان: محادثتان RAG، ملخص، ترجمة، كيانات LLM، تحليل موضوعات، خريطة، تحليل بحث، كيانات سريعة، وبحث أكاديمي. نجحت الجولتان دون 5xx أو تسرب نتيجة أو سقوط حاوية. بعد تحميل النماذج كانت الذاكرة: API نحو 76 MB، أداة LLM نحو 746 MB، الأداة السريعة نحو 426 MB، التوليد نحو 778 MB، embeddings نحو 990 MB، وStreamlit نحو 523 MB. رُفع حد العامل السريع إلى 768 MB بناءً على هذا القياس.

## أوامر التحقق

```powershell
docker compose config --quiet
docker compose exec -T api curl -fsS http://localhost:8000/ready
docker run --rm -v "F:\docker\files:/app" -w /app mysearchengine-app:latest sh -lc "pip install --no-cache-dir pytest >/dev/null && python scripts/verify_document_tools.py"
docker run --rm --network files_nlp-network -e PLATFORM_TEST_URL=http://api:8000 mysearchengine-app:latest python scripts/load_document_tools.py
docker run --rm --network files_nlp-network -e PLATFORM_TEST_URL=http://api:8000 mysearchengine-app:latest python scripts/mixed_10_user_acceptance.py
```

اختبار المتصفح موجود في `tests/e2e/browser_smoke.js`. اختبار دورة الحياة الحي موجود في `tests/integration/live_job_lifecycle.py`.

## حدود الإثبات المتبقية قبل نشر عام

- نتيجة العشرة مستخدمين مثبتة على جهاز التطوير الحالي وعلى ملف الاختبار المتاح ذي صفحتين. يلزم تكرار الحمل على مواصفات خادم الإنتاج وملف 50–100 صفحة لتثبيت أرقام السعة الخاصة بذلك الخادم.
- تم التحقق من إنشاء النسخة الاحتياطية وفهرسة dump وقراءة archive. لم يُنفذ restore مدمر فوق البيانات الحالية؛ يجب اختباره على نسخة staging أو volume مؤقتة.
- `/metrics` جاهز للمراقبة، لكن إرسال التنبيهات إلى منصة خارجية يحتاج اختيار منصة التشغيل وبياناتها.
- مفتاح OpenRouter ظهر في المحادثة/واجهة التطوير. يجب إلغاؤه وإصدار مفتاح جديد في `.env` قبل نشر الرابط مرة أخرى. لا يحتوي هذا التقرير أو source control على المفتاح.

هذه الحدود لا تمنع المراجعة البشرية للتنفيذ الحالي، لكنها تمنع وصف خادم غير معلوم الموارد بأنه production-certified أو اعتبار المفتاح المكشوف صالحًا للنشر.
