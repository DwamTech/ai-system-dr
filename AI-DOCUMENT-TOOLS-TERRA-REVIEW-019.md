# AI-DOCUMENT-TOOLS-TERRA-REVIEW-019

**التاريخ:** 2026-09-06  
**الفرع:** `conference-v1`  
**النطاق:** مراجعة تنفيذ `AI-DOCUMENT-TOOLS-TERRA-EXECUTION-PLAN-017.md` مقابل الكود وقاعدة البيانات والحاويات واختبارات API حية  
**الحكم:** **PARTIAL_IMPLEMENTATION — NOT_READY_FOR_FINAL_RELEASE**

## الحكم التنفيذي

نفّذ Terra الأساس المعماري الصحيح وأغلق سبب العطل الأصلي: الأدوات في وضع المنصة لم تعد تحلل placeholder، وأصبح لكل إصدار منشور artifact محفوظ، ولكل أداة Job خاص ونتيجة خاصة بصاحب الجلسة. هذه نقلة حقيقية ومهمة.

لكن التقرير النهائي المطلوب باسم `AI-DOCUMENT-TOOLS-IMPLEMENTATION-018.md` لم يُنشأ، ولم تُنفذ بوابة القبول في الخطة 017. جودة الأدوات نفسها ما زالت جزئية، ولا يجوز إعلان أن الأدوات الست تعمل بطلاقة مع أي ملف أو أن حمل عشرة مستخدمين مختلط قد اجتاز الاختبار.

النتيجة الحالية مناسبة كأساس تطوير، وليست إصدارًا نهائيًا. توجد خمسة موانع إطلاق مباشرة:

1. إلغاء وظيفة عاملة أو فقد العامل قد يترك Job عالقًا بلا استرداد تلقائي.
2. المقارنة في التحليل placeholder صريح وليست مقارنة فعلية.
3. عقود الإدخال والإخراج غير صارمة بما يكفي؛ طلب صفحة ترجمة بلا رقم قُبل ثم فشل كخطأ داخلي.
4. مسارات LLM في الكيانات والموضوعات والخريطة تمرر النص الكامل بلا ميزانية tokens أو map/reduce، ولا تضمن ملفات طويلة.
5. لا توجد اختبارات Unit/Contract/E2E/chaos داخل المستودع ولا CI يثبت السلوك.

## ما تم تنفيذه بصورة صحيحة

| المجال | الدليل | الحكم |
|---|---|---|
| artifact دائم | `DocumentArtifact` مع gzip وSHA-256 وكتابة atomic ومسار آمن | منفذ كأساس |
| backfill | المستند المؤرشف الحالي أصبح `ready`: صفحتان، 464 كلمة | منفذ للحالة الحالية |
| Tool Jobs | API لإنشاء الوظيفة والنتيجة والتنزيل، و`ToolExecution` وOutbox | منفذ كأساس |
| عزل النتائج | طلب نتيجة Job من workspace آخر أعاد 404 | ناجح |
| idempotency المتسلسل | إعادة نفس `request_id` والمدخلات أعادت نفس Job مع `deduplicated=true` | ناجح |
| ضغط الرجوع | `MAX_TOOL_QUEUE` و`MAX_OWNER_TOOL_JOBS` وعامل مخصص بتزامن 2 | منفذ |
| واجهة المنصة | التبويبات الست ترسل IDs وخيارات إلى API بدل تشغيل LLM داخل Streamlit في platform mode | منفذ جزئيًا |
| التحليل الحتمي | 20 وظيفة عبر جولتين من 10 workspaces اكتملت كلها؛ زمن الجولتين 1.79 و1.62 ثانية | ناجح لمسار لا يستدعي LLM |
| صحة التشغيل | `docker compose config --quiet` و`py_compile` نجحا، وكل الخدمات الاثنتي عشرة Healthy | ناجح وقت المراجعة |
| نظافة الأسرار والبيانات | `.env` و`data/` ignored وغير متتبعين في Git | ناجح داخل المستودع |

## أدلة تمنع إعلان الجاهزية

### 1. الإلغاء والتعافي غير صحيحين

- `still_owned()` يرفض Job حالته `cancel_requested`.
- عند ملاحظة العامل للإلغاء، يرمي `ToolFailure("cancelled")`، ثم يحاول `run_tool()` استخدام `update_job()` لتحويله إلى `cancelled`.
- `update_job()` يستدعي `still_owned()` فيرفض التحديث نفسه، فتظل الحالة `cancel_requested`.
- مع `task_reject_on_worker_lost=True` يعيد Celery الرسالة بعد موت العامل، لكن `claim_job()` يرفضها ما دام lease القديم صالحًا. الرسالة المعادة تنتهي بلا تنفيذ، ولا توجد sweeper تعيد نشرها بعد انتهاء lease؛ لذلك يمكن أن تبقى الوظيفة `running` إلى أجل غير محدد.
- حفظ ملف النتيجة، تحديث `ToolExecution`، وإنهاء `Job` ثلاث خطوات منفصلة بلا مسار reconciliation؛ الانقطاع بين الخطوات قد يعيد حسابًا مدفوعًا أو يترك نتيجة يتيمة.

**الحكم:** P0.

### 2. تحقق الإدخال يسمح بفشل يمكن منعه

اختبار حي أرسل ترجمة بـ`scope=page` دون `page`:

- POST `/tool-jobs`: أعاد 200 وأنشأ Job.
- العامل: رفع `ValueError("No selected pages")`.
- الحالة النهائية: `failed / internal_error` مع Traceback في السجل.

يجب رفض الطلب بـ422 قبل إنشاء Job، وبنفس المنطق يجب التحقق من النطاق، ترتيب `start_page/end_page`، حدود الصفحات، UUIDs، أنواع الكيانات، وعدم تكرار إصدارات المقارنة.

**الحكم:** P0.

### 3. المقارنة ليست منفذة

اختبار حي مرر إصدارين إلى التحليل مع `compare=true`. النتيجة المكتملة كانت:

```json
{
  "shared_topics": [],
  "differences": ["المقارنة تحتاج مراجعة الموضوعات المستخرجة."]
}
```

كما يقبل العقد الإصدار نفسه مرتين باعتبارهما مستندين. هذا نجاح شكلي بنتيجة غير منجزة.

**الحكم:** P0.

### 4. عقود النتائج وجودة التغطية ناقصة

- لا توجد Pydantic result models لكل schema؛ يكفي وجود `schema_version` حتى تُحفظ النتيجة.
- `extract_json()` يلتقط نصًا من أول `{` إلى آخر `}` دون JSON schema أو repair مضبوط.
- ملخص `include_bullets` لا يغير prompt؛ يحول كل سطر في النص النهائي إلى عنصر بعد التوليد.
- citations الملخص هي مقتطف أول 180 حرفًا من أول خمس صفحات، وليست أدلة مرتبطة بادعاءات الملخص.
- reduce الملخص قد يتجاوز نافذة النموذج عند كثرة المقاطع.
- كيانات LLM وأقسام البحث، موضوعات التحليل، والخريطة الذهنية ترسل `full_text` كاملًا في طلب واحد.
- entity types مرسلة في العقد لكنها غير مستخدمة في handler.
- `keep_formatting` في الترجمة غير مستخدم، ولا توجد لغة مصدر أو glossary أو checkpoints.
- الخريطة لا تتحقق من عدد العقد أو parent IDs أو cycles، وتحوّل فشل البنية إلى `internal_error`.
- حذف الصفحات الفارغة أثناء بناء artifact يعيد ترقيم الصفحات؛ لذلك قد تختلف page refs عن رقم صفحة PDF الأصلي.

**الحكم:** P0 للنتائج الكاذبة أو غير المحدودة، وP1 لباقي دقة العرض.

### 5. واجهة الأدوات لا تكمل تجربة الوظيفة الدائمة

- `platform_tool_jobs` محفوظ في `st.session_state` فقط؛ لا تتم استعادة الوظائف من `/jobs` بعد جلسة جديدة أو refresh كامل.
- لا يُمنع إرسال طلب مكرر أثناء Job نشط، ولا يوجد مسار Retry واضح بعد الخطأ.
- واجهة الترجمة حذفت scope للصفحة/النطاق والأسلوب وخيار التنسيق، رغم وجودها في العقد.
- اختيار أنواع الكيانات غير موجود.
- التحليل والخريطة يعرضان JSON خامًا، ولا توجد renderers للخريطة.
- تحليل نتائج البحث `web_analysis` موجود في العامل ولا يوجد زر أو عرض له في الواجهة.
- الواجهة تولد تنزيل JSON محليًا ولا تستخدم endpoint التنزيل؛ `PlatformClient._request()` يفترض JSON دائمًا، لذلك تنزيل TXT/Markdown سيفشل عند استخدامه.
- `st.stop()` بعد رسم التبويبات يعزل الكود القديم، لكنه مسار هش يكرر حقن dashboard/cookie ويمنع أي كود لاحق بلا عقد واضح.

**الحكم:** P0 للاستعادة ومنع التكرار، وP1 لاكتمال تجربة كل أداة.

### 6. البحث الأكاديمي تحسن جزئيًا فقط

الموجود حاليًا: trim للاستعلام، كشف ar/en، اختيار Wikipedia صراحة، فلترة http/https، deduplication أساسي، حفظ النتائج في Job، وTTL للصحة.

المفقود: `requests.Session`، retry/backoff، circuit breaker حقيقي، اكتشاف capabilities، allowlist مبنية على المحركات المتاحة، normalization كامل لـDOI/URL/title، ranking، pagination، وإعداد proxy headers. عزل prompt injection قائم على جملة داخل prompt فقط ولا توجد بنية بيانات أو اختبارات هجوم تثبته.

**الحكم:** P1.

### 7. قاعدة البيانات والنشر والمراقبة

- الجداول الجديدة تُنشأ بـ`create_all` والأعمدة القديمة تُضاف بـ`ALTER TABLE` يدوي؛ لا توجد migration versioned أو downgrade/upgrade test.
- `ToolExecution.document_version_id` ليس Foreign Key، والإصدارات المتعددة موجودة داخل JSON فقط.
- healthcheck الخاص بكل Celery worker يبحث عن أي `pong` ولا يثبت أن العامل المقصود أو الـqueue المقصود متاح.
- API وtool-worker وStreamlit يعملون بصورة أحدث من generation/index workers في تعريف Docker الحالي؛ ملفات المهام متطابقة، لكن `web_search.py` مختلف بين الصور. هذا يكشف نشرًا غير ذري يمكن أن ينتج سلوكًا مختلطًا.
- `/metrics` لا يقدم completion/error/latency/queue wait/provider calls حسب الأداة.
- كل الأدوات تشترك في queue واحدة وتزامن 2؛ ترجمتان طويلتان تستطيعان تأخير تحليل حتمي أو بحث سريع.

**الحكم:** P1، مع P0 لمسار تعافي الوظائف.

## حالة عيوب التقرير 016

التصنيف هنا مبني على السلوك المطلوب كاملًا، لا على وجود ملف أو دالة بالاسم.

| المجموعة | مغلق | جزئي | مفتوح | النتيجة |
|---|---:|---:|---:|---|
| DT-P0 المشتركة | 1 | 3 | 0 | أصل الـplaceholder مغلق؛ العقود والتعافي والتغطية جزئية |
| SUM-01..07 | 3 | 4 | 0 | يعمل ويحفظ النتيجة، لكن الخيارات/التغطية/citations غير مكتملة |
| ENT-01..07 | 3 | 2 | 2 | fast تحسن؛ الجودة العربية والفallback وLLM schema ناقصة |
| TRN-01..08 | 3 | 2 | 3 | الصفحات الحقيقية موجودة؛ التنسيق والاستكمال وglossary ناقصة |
| ANL-01..07 | 2 | 1 | 4 | metrics الأساسية تعمل؛ الموضوعات والمقارنة والتطبيع والعرض ناقصة |
| MAP-01..09 | 1 | 4 | 4 | Job موجود؛ التغطية والتحقق والعرض والتصدير ناقصة |
| WEB-01..11 | 2 | 7 | 2 | حفظ وعزل أساسيان؛ resilience ودقة المصادر غير مكتملين |
| **الإجمالي** | **15** | **23** | **15** | **38 من 53 بندًا ما زالت جزئية أو مفتوحة** |

## ما لم يُثبت

- لا توجد ملفات `tests/` أو إعداد pytest/Playwright/CI في المستودع.
- لم يُختبر PDF من 50 صفحة، PDF مختلط RTL/LTR، ملف جداول، ملف تالف/محمي، أو نسختان بالاسم نفسه.
- لم يُختبر restart للعامل أثناء العمل، cancel أثناء LLM، 429/timeout/invalid JSON، توقف SearXNG وعودته، أو فساد artifact/result.
- لم يُختبر حمل مختلط لعشرة مستخدمين يشمل المحادثة والملخص والترجمة والخريطة والبحث مرتين.
- لم تُقاس الذاكرة وCPU وqueue wait وprovider latency تحت الحمل.
- لم تُجر Browser E2E لكل أداة بعد التنفيذ على desktop و768 و390 و375.

## القرار

لا يعتمد الإصدار الحالي كإغلاق نهائي للأدوات. يعتمد فقط كأساس معماري ناجح يحتاج تنفيذ `AI-DOCUMENT-TOOLS-CLOSURE-PLAN-020.md` بالترتيب، ومنع الانتقال بين مراحله قبل نجاح بوابة المرحلة.

## ملاحظة تشغيل وأمن

ظهر مفتاح مزود الذكاء في سياق المحرر سابقًا. يجب إلغاؤه وإصدار مفتاح جديد قبل النشر النهائي، مع إبقاء المفتاح الجديد خارج Git والسجلات. لم تتم طباعة المفتاح أو نسخه في هذه المراجعة.
