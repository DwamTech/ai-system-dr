# مراجعة تنفيذ خطة 10 مستخدمين — تاسكات الاستكمال

**التاريخ:** 2026-09-05  
**مرجع المتطلبات:** `AI-MULTIUSER-10-IMPLEMENTATION-PLAN-013.md`  
**مرجع ادعاءات التنفيذ:** `AI-MULTIUSER-10-IMPLEMENTATION-014.md`  
**الحكم:** `NOT_READY_FOR_10_USERS` — بنية أولية منفذة جزئيًا، بها عيوب مانعة ومراحل قبول غير مكتملة.  
**نطاق هذه الجولة:** مراجعة فقط. لم تُنفذ أي من تاسكات الإصلاح أدناه ولم يُعدّل كود التطبيق أو إعداداته.

## النتيجة الهندسية

وجود FastAPI وPostgreSQL وRedis مستقل وCelery وخدمة embeddings خطوة صحيحة، لكنه لا يثبت تنفيذ الخطة كاملة. اختبار التقرير 014 يثبت قبول عشرة طلبات وحفظها وإرسالها للطابور وعزل قراءة محادثة بمعرّف معلوم. لا يختبر تشغيل العمال وإكمال عشرة ملفات وردود، أو استمرار الخدمة عند فشلها، أو ساعة الحمل المطلوبة.

أخطر مانع تشغيل مؤكد: إعداد Celery الحالي لا يستورد `backend.tasks`، وبالتالي لا يسجل مهمتي الفهرسة والتوليد عند بدء العامل بأمر Compose. وهناك صلاحية حذف الفهرس العام ما زالت متاحة لأي زائر من الإعدادات، رغم شرط الخطة الخاص بحماية الإدارة.

الأرشيف يجب أن يظل عامًا. إصلاح العزل المقصود هنا يخص الهوية والمحادثات وإيصالات المهام وإلغائها، ولا يعني تحويل المستندات المنشورة إلى أرشيف خاص لكل مستخدم.

## طريقة المراجعة وحدود الدليل

- قراءة الخطة والكود الحالي في `backend/` و`app_optimized.py` و`platform_client.py` والمحرك والمعالج والمزود وCompose وتقارير التنفيذ.
- تنفيذ مقتطفات اختبار في حاويات مؤقتة بالصورة المحلية، مع ملفات المشروع مركبة للقراءة فقط، والشبكة معطلة، وSQLite في `/tmp` مؤقت. لم يُستخدم مفتاح مزود ولم تُكتب بيانات في الخدمات القائمة.
- فحص تسجيل المهام كما يفعل محمّل Celery، وفحص مصدر constructor الخاص بمكتبة OpenSearchVectorSearch المثبتة محليًا.
- اختبار API عبر TestClient ودوال العامل ببدائل داخل الذاكرة عند الحاجة. هذا يثبت العيوب المحددة أدناه، ولا يعوض اختبار PostgreSQL/OpenSearch/Celery الحقيقي المتكامل.
- فحص الدالة الخاصة بإكمال الفهرسة باستخراجها من AST وتشغيلها منفردة؛ ليس اختبار متصفح للواجهة كاملة.
- عند المراجعة كانت الحاويات العاملة: Streamlit وOpenSearch وRedis cache وSearXNG فقط. خدمات المنصة الجديدة لم تكن عاملة؛ لم أبدأها أو أعد تشغيل التطبيق خلال هذه المراجعة.
- أرقام الأسطر التالية تخص نسخة الكود وقت المراجعة؛ يُستخدم اسم الدالة أيضًا للوصول عند تغير الأسطر.

### نتائج الاختبارات المعزولة

| الفحص | النتيجة الفعلية | التاسك |
|---|---|---|
| `celery_app.loader.import_default_modules()` | `registered_application_tasks=[]` و`imports=()` و`include=()` | MU10-01 |
| مصدر `OpenSearchVectorSearch.__init__` | ينشئ clients فقط؛ لا ينشئ index أو vector mapping | MU10-14 |
| سؤالان في المحادثة نفسها قبل انتهاء الأول | قُبل الاثنان بحالة HTTP 200 | MU10-12 |
| نفس request_id في محادثة ثانية لنفس المالك | رجع job المحادثة الأولى | MU10-12 |
| مستخدمان يرفعان PDF مطابقًا | رجع لهما نفس job؛ قراءة الثاني له أعادت 404 | MU10-13 |
| API تسجل إلغاء وعامل يحتفظ بكائن Job سابق | API: `cancel_requested`؛ العامل: `is_cancelled=False`؛ تحديثه التالي أعاد الحالة إلى `indexing` | MU10-17 |
| إرسال `status=completed` لدالة إكمال الفهرسة | ظلت `tour_index_completed=False` | MU10-05 |
| فشل مزود ثم تكرار السؤال بعد تهيئة نجاح بديل | عدد محاولات التنفيذ بقي 1؛ رُدّ الفشل المخزن | MU10-18 |
| محاكاة موت العامل بعد حفظ الإجابة وقبل إكمال Job ثم إعادة التسليم | رسالتا assistant لنفس المهمة، والمهمة `completed` | MU10-16 |

## قائمة التنفيذ المقترحة — جميع البنود غير منفذة

**P0:** مانع تشغيل أساسي أو خطر مباشر على بيانات الجميع.  
**P1:** لازم لتحقيق الاستقرار والعزل وسيناريوهات الخطة.  
**P2:** استكمال ضبط التشغيل وقابلية القياس؛ يدخل ضمن شروط التسليم النهائي.

### [x] MU10-01 — P0 — تسجيل مهام Celery عند تشغيل العامل

- **الدليل:** `backend/celery_app.py:14` ينشئ Celery بلا `include` أو `imports` أو autodiscovery. `backend/tasks.py:101,208` يحتوي التعريفات، لكن أوامر العامل في `docker-compose.yml:172,188` تستورد وحدة celery_app فقط. اختبار محمّل Celery أكد عدم وجود مهام التطبيق المسجلة.
- **الأثر:** قد تنجح API وdispatcher وتظل قاعدة البيانات على queued بينما العامل يرفض رسائل المهام غير المسجلة. اختبار import اليدوي لـtasks في التقرير السابق لا يتحقق من مسار بدء العامل الحقيقي.
- **المطلوب:** تسجيل الوحدة صراحة في إعداد العامل، والتحقق من طابوري indexing وgeneration عند startup.
- **شرط القبول:** بدء العامل بنفس أمر Compose وإظهار المهمتين في registered tasks، ثم تنفيذ مهمة PDF ومهمة سؤال إلى حالة نهائية عبر API → outbox → broker → worker، دون import يدوي إضافي في الاختبار.

### [x] MU10-02 — P0 — منع الزائر من حذف الأرشيف العام

- **الدليل:** `app_optimized.py:1321` يعرض زر مسح الفهرس لكل زائر، و`:1329` يستدعي `clear_database()` مباشرة. `engine_optimized.py:243` يحذف الفهرس بلا تحقق صلاحية. هذه فجوة سابقة ظلت موجودة رغم متطلبات الخطة §4.
- **الأثر:** زائر يهيئ المحرك من الإعدادات يستطيع حذف مستندات الجميع. سجلات PostgreSQL المنشورة تبقى، فتختلف حالة الأرشيف عن بيانات البحث.
- **المطلوب:** نقل عمليات الحذف والإدارة إلى مسار إداري بصلاحية خادمية، ومزامنة أثره على سجل الإصدارات والكاش. إخفاء الزر وحده ليس تحقق صلاحية.
- **شرط القبول:** جلسة ضيف لا تستطيع حذف الفهرس من UI أو API؛ اختبار إداري معزول يثبت اتساق السجل والبحث. لا يُختبر الحذف على أرشيف المستخدم الفعلي.

### [ ] MU10-03 — P1 — هوية جلسة آمنة لا تنتقل بمجرد مشاركة الرابط

- **الدليل:** `app_optimized.py:164` يقرأ bearer token من `workspace` ويكتبه في query string. `backend/api.py:109` يقبل token يختاره العميل. `expires_at` موجود في النموذج ولا يُتحقق منه في `owner_from_token()`.
- **الأثر:** نسخ رابط الصفحة الكامل لشخص آخر ينقل له هوية المالك، ومنها الوصول إلى مهمته الموجودة في `index_job`. التخزين في URL يخالف أسلوب cookie الموقعة المطلوب في الخطة.
- **المطلوب:** إصدار الهوية من الخادم وحفظها في cookie آمنة موقعة مع سياسة انتهاء، وإزالة الأسرار من الروابط. إبقاء رابط التطبيق قابلًا للمشاركة مع إنشاء جلسة مستقلة لكل متصفح.
- **شرط القبول:** نسخ الرابط بين متصفحين مستقلين لا ينقل الهوية؛ إعادة تحميل متصفح صاحبه تحفظها؛ تنتهي الجلسات وفق السياسة، مع استمرار قراءة الأرشيف العام.

### [x] MU10-04 — P1 — استرجاع المحادثة والطلبات بعد refresh وإعادة الاتصال

- **الدليل:** `app_optimized.py:177` يعيد تهيئة `indexing_job_ids` و`conversation_id` و`chat_job_id` داخل Session State فقط. `ensure_platform_conversation():567` ينشئ محادثة جديدة إذا ضاع المعرف. لا يوجد API لقائمة محادثات المالك أو مهامه واستعادة الجاري منها.
- **الأثر:** البيانات قد تبقى في PostgreSQL، لكن الواجهة تفقد طريق الوصول إلى المحادثة والرد الجاري عند تحديث كامل للصفحة؛ الاحتفاظ بالـtoken وحده لا يستعيدهما.
- **المطلوب:** عقود لاسترجاع محادثات ومهام المالك، وربط اختيار المحادثة/النطاق بطريقة قابلة للاستعادة مع التحقق من الملكية.
- **شرط القبول:** refresh أثناء توليد رد أو فهرسة عدة ملفات، وفصل الشبكة، وإعادة تشغيل UI؛ تظهر نفس المحادثة والمهمة والنتيجة دون إنشاء محادثة أو طلب مكرر.

### [x] MU10-05 — P1 — تصحيح عقد اكتمال الفهرسة والجولة

- **الدليل:** `app_optimized.py:663` يشترط `status.get('state') == 'completed'` قبل فرع المنصة، بينما `backend/api.py:71` يعيد المفتاح `status`. `draw_platform_status():717` يمرر الرد كما هو. الاختبار المعزول أثبت خروج الدالة دون تحديث الحالة.
- **الأثر:** بطاقة النجاح قد تظهر، لكن الجولة لا تسجل نجاح الفهرسة ولا تُحدّث قائمة المستندات من هذا المسار.
- **المطلوب:** توحيد DTO الحالات أو عمل adapter صريح للمسار القديم والجديد، مع تحديث workflow والجولة والأرشيف مرة واحدة بعد انتهاء المهمة.
- **شرط القبول:** فهرسة حقيقية تنقل الجولة من خطوة الفهرسة إلى اختيار المستند وتظهر الملف في الاختيار دون تهيئة يدوية أو refresh كامل؛ لا يحدث loop من reruns.

### [x] MU10-06 — P1 — حفظ وعرض حالة كل ملفات الدفعة

- **الدليل:** `app_optimized.py:804` يجمع المعرفات محليًا ثم يحفظها عند نهاية الحلقة فقط، ويختار `accepted_jobs[-1]` في `:827`. العرض يتابع `indexing_job_id` واحدًا. لا توجد قائمة مهام مستديمة للمالك في API.
- **الأثر:** فشل قبول الملف الثاني بعد نجاح الأول يفقد الواجهة إيصال الأول؛ متابعة الملف الأخير وحده تخفي فشل الملفات السابقة. عدم وجود accepted_jobs يعتبر نجاح dedup حتى لو استُبعدت كل الملفات في التحقق الثاني.
- **المطلوب:** حفظ كل إيصال فور قبوله، وعرض حالة كل ملف وحالة إجمالية محسوبة للدفعة، والتفريق بين duplicate جاهز ورفض الملف أو فشل إرساله.
- **شرط القبول:** دفعة من ثلاثة ملفات، أحدها يرفض/يفشل؛ تظهر نتائج الثلاثة وتُستعاد بعد refresh؛ لا تظهر رسالة اكتمال كامل بسبب نجاح آخر ملف فقط.

### [ ] MU10-07 — P1 — تحديث الأرشيف العام لكل الجلسات وتوحيد مصدره

- **الدليل:** `load_indexed_files_from_db():224` يحمل القائمة مرة واحدة خلف `db_files_loaded`. لا يوجد archive revision أو استطلاع لتغييرات الآخرين. `show_archive_dialog():1348` ما زال يعتمد على rag_engine وقائمة أسماء من OpenSearch. `/documents` يعيد جميع السجلات بلا بحث أو pagination.
- **الأثر:** جلسة مفتوحة قد لا ترى ملفًا نشره مستخدم آخر، ونافذة الأرشيف قد تطلب تهيئة محرك رغم توفر API والأرشيف.
- **المطلوب:** جعل سجل الإصدارات المنشورة مصدر العرض الوحيد، مع pagination وبحث وآلية تحديث محدودة الحمل، وتحديث القائمة في كل الجلسات النشطة.
- **شرط القبول:** ينشر A ملفًا فيراه B خلال 5 ثوانٍ دون إعادة فهرسة أو تحميل الصفحة؛ صفحة أرشيف كبيرة لا تُرسل كل السجلات في كل استطلاع.

### [ ] MU10-08 — P1 — استخدام document_id/version_id في الواجهة والأدوات والمصادر

- **الدليل:** `app_optimized.py:229` يستعمل الأسماء كقائمة، و`:230` يبني `name → version_id`، و`_available_documents():862` يزيل التكرارات حسب الاسم. `engine_optimized.py:541` يجلب النص باسم source، و`:324` يزيل تكرار المصادر بالاسم.
- **الأثر:** ملفان مختلفان باسم واحد ينهاران إلى خيار واحد أو يرتبطان بالإصدار الخطأ، والأدوات قد تجمع مقاطع الملفين رغم وجود version_id في قاعدة البيانات.
- **المطلوب:** حمل المعرفات كقيم الاختيار، واستخدام الاسم للعرض فقط مع تمييز بصري عند التكرار. تمديد المصادر وجلب النص إلى معرف الإصدار والصفحة.
- **شرط القبول:** ملفان باسم واحد وعلامتان مختلفتان؛ اختيار كل ملف يعطي محتواه وحده في المحادثة والتلخيص والترجمة والمصادر.

### [ ] MU10-09 — P1 — توفير المحتوى الفعلي للأدوات بعد الفهرسة المستقلة

- **الدليل:** `backend/tasks.py:143` يتخلص من `_raw_text` و`_pages` دون حفظهما. `app_optimized.py:235` يضع placeholder يبدأ بـ`[مستند مفهرس` بينما `get_file_content_safe():259` يتعرف على `[ملف مفهرس` فقط. مسار المنصة لا يهيئ rag_engine الذي تعتمد عليه أدوات مثل الملخص في `:2182`.
- **الأثر:** قد تُعطل الأدوات أو تعالج عبارة placeholder بدل نص المستند. ربط المحادثة بالـAPI وحده لا يحافظ على الأدوات السبعة.
- **المطلوب:** حفظ النص والصفحات مرة واحدة حسب الإصدار، وإتاحتها عبر عقد جلب محدود، وإزالة النص الوهمي كبديل للمحتوى. جعل جاهزية الأدوات تعتمد على الخدمات اللازمة لها.
- **شرط القبول:** بعد فهرسة PDF نصي وPDF OCR، تعمل الأدوات على النص الصحيح في جلسة جديدة لم ترفع الملف، مع حدود تحميل تمنع نسخ الأرشيف كاملًا لكل جلسة.

### [x] MU10-10 — P1 — منع العودة الصامتة إلى التنفيذ المحلي عند عطل المنصة

- **الدليل:** `app_optimized.py:168` يحاول إنشاء client مرة واحدة؛ عدة مسارات تضبط `platform_available=False` ولا تستعيدها تلقائيًا. `start_background_indexing():837` يرجع للخيط القديم. بعد فشل send_prompt في `:1759` يمكن أن يمر التنفيذ إلى المسار المحلي غير المشروط في `:1764`. `platform_client.py:31` يجمع أخطاء 429 و401 و5xx والاتصال في استثناء واحد.
- **الأثر:** تعطل عابر أو رفض سعة قد يعيد نفس الاختناقات القديمة ويتجاوز طابور القبول، أو يكرر طلبًا قبله الخادم وفُقد رده. الواجهة قد تظل في fallback بعد تعافي الخدمة.
- **المطلوب:** اختيار وضع التشغيل بإعداد صريح، وفصل أنواع الأخطاء. في وضع multiuser تُحفظ نية الطلب ومعرفه وتظهر حالة الانتظار/الرفض وتُستعاد الخدمة دون تشغيل بديل محلي.
- **شرط القبول:** 429 وانقطاع API بعد commit وقبل الرد لا ينتجان أي تنفيذ خارج الطابور أو رسالة مكررة؛ تعود المتابعة تلقائيًا عند التعافي.

### [x] MU10-11 — P1 — قبول ذري وعادل وحدود لكل مستخدم ومهلة تشمل الانتظار

- **الدليل:** `backend/api.py:174,323` يحسب COUNT ثم يضيف المهمة بلا حجز سعة ذري. لا توجد حصة لكل مالك أو scheduler عادل أو deadline. generation queue=80 في Compose مقابل 20 في بداية الخطة، والتنفيذ=2 مقابل 4 دون قياس يبرر الاختيار.
- **الأثر:** الطلبات المتزامنة قد تتجاوز السعة، ومستخدم واحد يستطيع شغل الطابور كله. مهلة Celery تبدأ عند التنفيذ ولا تحد انتظار المهمة إذا غاب العامل.
- **المطلوب:** حجز سعة بمعاملة/قيد مناسب، وحدود لكل مستخدم ودفعة وحجم إجمالي، وجدولة عادلة بين المستخدمين والأنواع، ومهلة نهائية تشمل الانتظار مع 429/503 وRetry-After مناسبين. عاير عدد workers بدل افتراض أن زيادة العدد إصلاح.
- **شرط القبول:** اختبار PostgreSQL متزامن عند آخر slot لا يتجاوز الحد؛ مستخدم يرسل دفعة كبيرة لا يمنع التسعة الآخرين؛ كل مهمة تنتهي أو تُرفض بوضوح ضمن المهلة المعلنة.

### [x] MU10-12 — P1 — ترتيب المحادثة ومنع تكرار إرسال السؤال

- **الدليل:** `backend/api.py:319` يبحث request_id حسب المالك دون conversation_id. لا يوجد منع طلب جارٍ آخر في المحادثة. `:346` لا يعالج سباق القيد الفريد. الواجهة تولد UUID جديدًا لكل محاولة في `app_optimized.py:1748`. العامل في `backend/tasks.py:225` يقرأ آخر رسائل وقت التنفيذ بما فيها السؤال الجاري وربما أسئلة أحدث.
- **الأثر:** إعادة الطلب عبر محادثتين قد تربط المهمة بالمحادثة الخطأ، وإرسال سؤالين بسرعة يخل بترتيب السياق. الاختبارات أثبتت قبول سؤالين جاريين ورجوع مهمة المحادثة الأولى للثانية.
- **المطلوب:** idempotency على `(owner, conversation, client_request_id)` مع حفظ معرف العميل قبل الإرسال ومعالجة السباق ذريًا، ومنع التوازي داخل المحادثة/المستخدم حسب السياسة، وتحديد cutoff للسياق وقت القبول.
- **شرط القبول:** إعادة إرسال متزامنة لنفس الطلب تعيد الإيصال نفسه بلا 500 أو تكرار؛ معرف مماثل في محادثة أخرى لا يرجع مهمتها؛ السؤال الثاني يستخدم ترتيبًا صحيحًا بعد الأول.

### [x] MU10-13 — P1 — dedup يحافظ على ملكية الإيصالات وإعدادات الإصدار

- **الدليل:** `backend/api.py:187` و`:232` يرجعان مهمة موجودة دون شرط مالك؛ `GET /jobs` يتحقق من المالك، فحصل المستخدم الثاني في الاختبار على 404. `db.flush():200` خارج try الخاص بـIntegrityError، فلا يغطي catch سباق إنشاء document نفسه. إعادة الاستخدام تعتمد content_hash/status فقط ولا تقارن model_revision/chunking/force_ocr؛ API تثبت إعدادات chunking في `:213` رغم خيارات الواجهة.
- **الأثر:** كسر متابعة المستخدم الثاني وكشف تفاصيل مهمة غير مملوكة في رد القبول، واحتمال 500 عند رفع متزامن، وإعادة استخدام معالجة قديمة رغم تغيير الإعدادات.
- **المطلوب:** فصل العمل المشترك عن إيصال/مهمة كل مستخدم، وضبط dedup على المحتوى وإعدادات المعالجة وإصدار النموذج، ومعاملة تغطي الإنشاء والـflush، وإلغاء إيصال المستخدم دون إلغاء مستفيد آخر.
- **شرط القبول:** اثنان يرفعان نفس المحتوى في اللحظة نفسها؛ لكل منهما متابعة وإلغاء مستقلان، وتنفيذ فهرسة مشترك واحد عند تطابق الإعدادات؛ تغيير الإعدادات ينشئ إصدارًا جديدًا مناسبًا.

### [x] MU10-14 — P1 — إنشاء mapping الفهرس العام قبل النشر

- **الدليل:** `publish_staged_version():85` يفترض أن `shared_engine('public').get_vectorstore()` ينشئ فهرسًا وmapping. مصدر constructor المثبت أثبت أنه ينشئ clients فقط. ثم ينفذ reindex نحو اسم الفهرس العام مباشرة.
- **الأثر:** عند أول تشغيل على مخزن فارغ، إنشاء الوجهة التلقائي قد يجعل vector_field حقلاً عاديًا بدل knn_vector، أو يفشل النسخ إذا كان الإنشاء التلقائي ممنوعًا. وجود الفهرس القديم يخفي العيب في الاختبارات.
- **المطلوب:** تهيئة mapping/settings صراحة وبإصدارات وأبعاد متجهات متوافقة، والتحقق من الفهرس الموجود دون حذفه تلقائيًا.
- **شرط القبول:** مخزن OpenSearch تجريبي فارغ يقبل أول ملف ثم يجيب عبر البحث المتجهي؛ كذلك ينجح التحقق على فهرس قديم متوافق ويعطي مسار ترقية واضحًا عند عدم التوافق.

### [x] MU10-15 — P1 — نشر الإصدار بصورة تمنع البحث في الأجزاء غير المكتملة

- **الدليل:** `backend/tasks.py:96` ينسخ المقاطع بـreindex قبل commit سجل النشر في `:186`. `engine_optimized.py:368` يبحث في كل الفهرس عند غياب النطاق بلا قائمة الإصدارات المنشورة. فحص نتيجة النسخ لا يقارن عدد المقاطع المتوقع ولا يثبت قابلية البحث. تغيير current_version_id يبدأ من قبول المهمة في `backend/api.py:207`.
- **الأثر:** reindex ليس نقطة نشر ذرية لكل المستند؛ نسخ جزء ثم فشل قد يترك مقاطع قابلة للبحث رغم فشل Job وعدم نشر السجل. لا يكفي اسم staging لحماية «كل المستندات».
- **المطلوب:** فرض فلتر الإصدارات المنشورة على جميع مسارات الاسترجاع، والتحقق من عدد المقاطع ونتائج bulk وقابلية البحث قبل تبديل الإصدار المنشور، وإبقاء الإصدار القديم متاحًا أثناء تجهيز بديله، مع archive_revision وتنظيف المقاطع غير المنشورة وفق سياسة.
- **شرط القبول:** فشل النسخ بعد جزء من المقاطع وقتل العامل قبل commit لا يظهران أي جزء جديد في إجابات جميع المستندات؛ يظل الإصدار السابق متاحًا؛ نجاح النشر يُظهر الإصدار كاملًا.

### [x] MU10-16 — P1 — استعادة المهام ومنع تكرار الأثر عند إعادة التسليم

- **الدليل:** `backend/dispatcher.py:32` يقرأ outbox بلا claim/lock؛ النشر للوسيط يحدث قبل commit `published_at`. `backend/tasks.py:105,212` لا يحجز lease أو fencing token. الإجابة تُحفظ في commit مستقل `:240` ثم تكتمل المهمة في `:241`، ورسالة assistant بلا request_id فريد. الاختبار أنتج ردين بعد محاكاة موت العامل في هذه الفجوة. لا توجد checkpoints قابلة للاستعادة أو sweeper للمهام العالقة أو retry_wait وحد محاولات استعادة.
- **الأثر:** إعادة التسليم المتوقعة قد تضاعف الردود/التكلفة أو تنفذ المهمة في عاملين. heartbeat يتحدث عند مراحل فقط، ولا توجد استعادة مبنية عليه. انتهاء العملية بالقوة لا يضمن حالة نهائية سليمة في PostgreSQL.
- **المطلوب:** claim/lease مع fencing، وidempotency للأثر، ومعاملة واحدة لحفظ الإجابة وحالة الإكمال، ونقاط استعادة وفحص للمهام العالقة، وسياسة retries محدودة وvisibility timeout معلن. يجب تحمل فجوة outbox بدل افتراض تسليم مرة واحدة.
- **شرط القبول:** قتل العامل عند كل حد commit وإعادة تسليم الرسالة وتشغيل dispatcher آخر لا تنتج إلا إجابة واحدة/إصدار منشور واحد؛ كل مهمة تستعاد أو تنتهي بفشل معلوم دون حلقة إعادة تنفيذ غير محدودة.

### [x] MU10-17 — P1 — تنفيذ إلغاء قابل للتحقق وعدم الكتابة فوقه

- **الدليل:** `backend/db.py:26` يستخدم `expire_on_commit=False`. `is_cancelled():81` يقرأ Job من identity map دون refresh؛ الاختبار أثبت فقد الإلغاء وتغييره لاحقًا إلى indexing. `generate_answer()` يفحص الإلغاء قبل البدء فقط، ولا يفحصه بعد المزود وقبل حفظ الرد. الإلغاء لا يحدّث DocumentVersion في المسارات الطرفية.
- **الأثر:** الزر يمكن أن يوحي بالإلغاء بينما يستمر النشر/حفظ الرد؛ إعادة التقدم تمحو cancel_requested. حالة إصدار قد تبقى preparing/indexing بعد إلغاء المهمة أو timeout.
- **المطلوب:** قراءة إلغاء حديثة من DB، وانتقالات مشروطة لا تكتب فوق terminal/cancel state، ونقاط إلغاء أثناء العمل وقبل النشر/حفظ الرد، ومصالحة حالة الإصدار. دعم زر إلغاء الرد ومعالجة cancel_requested في UI أيضًا.
- **شرط القبول:** إلغاء queued وOCR وembedding وطلب مزود جارٍ ينتهي بحالة واضحة؛ لا تُنشر نتيجة ملغاة ولا تتغير حالة مستخدم آخر، ولا يبقى إصدار معلقًا.

### [x] MU10-18 — P1 — فشل المزود لا يُسجل كإجابة ناجحة ولا يدخل cache

- **الدليل:** `engine_optimized.py:297` يحول الاستثناء لنص ثم يخزنه في `:305`. `backend/tasks.py:230` يعتبر أي tuple جوابًا مكتملًا. الاختبار أثبت أن محاولة السؤال الثانية لم تصل للمزود بعد فشل الأولى.
- **الأثر:** رسالة خطأ تظهر كمهمة completed وتستمر حتى بعد تعافي المزود؛ نسب نجاح النظام تصبح مضللة ويضيع مسار إعادة المحاولة الصحيح.
- **المطلوب:** نتيجة typed أو استثناء يحفظ سبب الفشل بصورة آمنة، ويُحدث Job/message إلى failed أو retry_wait حسب السياسة، مع تخزين الإجابات الناجحة فقط.
- **شرط القبول:** مزود يعيد 429/timeout ثم يتعافى؛ الطلب الأول ليس completed ناجحًا، وإعادة المحاولة تصل للمزود وتنتج جوابًا فعليًا.

### [x] MU10-19 — P1 — cache مرتبط بالنشر والهوية ومحدود الذاكرة

- **الدليل:** `engine_optimized.py:269` لا يتضمن owner أو archive_revision أو إعدادات التوليد. `_fallback_cache` في المحرك dict بلا TTL أو حد حجم. مسح Redis أثناء فهرسة staging في `:230` لا يمس caches عمليات generation، وقد تظل ردود محلية قديمة حتى بعد النشر.
- **الأثر:** سؤال «كل المستندات» قد يظل يجيب ببيانات قبل إضافة ملف جديد؛ تزداد ذاكرة العمال مع طول التشغيل. سياسة cache الخاص بالمستخدم المطلوبة لم تُنفذ.
- **المطلوب:** تضمين الهوية ونطاق الإصدارات/مراجعة الأرشيف وإعدادات النموذج في المفتاح، وcache محلي محدود الحجم والزمن، وإبطال بعد نجاح النشر لا قبله، مع فصل cache النصوص العامة عن الردود الخاصة.
- **شرط القبول:** نفس السؤال قبل وبعد نشر ملف جديد يعكس الإصدار الجديد في كل workers؛ توقف Redis لا يؤدي إلى نمو ذاكرة غير محدود أو بقاء إجابة قديمة بلا نهاية.

### [ ] MU10-20 — P1 — إكمال بوابة التوليد والأدوات السبعة خارج UI

- **الدليل:** `app_optimized.py:340,497,512,535,2367,2586,2718,2989` ما زال يستدعي LLM مباشرة؛ الملخص كذلك عبر المحرك في `:2192`. المهام الخلفية تشمل index_document وgenerate_answer فقط. `openrouter_client.py:104` لا يملك حد تزامن/معدل مركزيًا أو circuit breaker؛ retry هو sleep ثابت دون Retry-After أو jitter أو deadline شامل.
- **الأثر:** حد عامل الردود لا يحد إجمالي طلبات المفتاح عند استخدام الترجمة/التحليل وغيره؛ مستخدمون في تبويبات مختلفة يعيدون ضغط المزود والواجهة.
- **المطلوب:** مرور جميع استدعاءات المزود وإعادة الصياغة عبر بوابة موحدة محدودة السعة والمعدل والميزانية، وطوابير عادلة للأدوات، ونقل NLP الثقيل أيضًا خارج UI مع حفظ النتائج وحالات الفشل.
- **شرط القبول:** اختبار مختلط للأدوات السبعة يثبت عدم وجود استدعاء يتجاوز الحد العالمي، واستجابة الواجهة أثناء العمل، واحترام 429 وRetry-After وسقف إعادة المحاولة.

### [x] MU10-21 — P1 — جاهزية embeddings بعد التحميل وجدولة تمنع اختناق البحث

- **الدليل:** `backend/embeddings_api.py:26` يحمل النموذج lazily عند أول /embeddings، بينما `/health:36` يعيد ok حتى `loaded=False`. Semaphore ينتظر دون حد طابور أو timeout/أولوية، ولا يوجد ضبط threads الداخلية أو revision محدد. `backend/embeddings.py:18` مهلة العميل 45 ثانية قد تنتهي أثناء التحميل الأول أو انتظار الطابور.
- **الأثر:** الخدمات تبدو healthy قبل جاهزية النموذج، وقد تتكرر مشكلة الانتظار الطويل عند البداية. دفعات الفهرسة والبحث تتنافس دون ضمان عدالة.
- **المطلوب:** warm-up ضمن readiness مع liveness مستقل، وتثبيت النموذج، وضبط threads والتوكنات والدفعات، وطابور bounded بأولوية بحث لا تجوّع الفهرسة، وحالة overload واضحة.
- **شرط القبول:** readiness لا تنجح قبل embedding تجريبي ناجح؛ تنزيل أولي وفهرسة مع عشرة أسئلة لا يؤديان إلى قبول يتبعه timeout خفي بسبب التحميل؛ يُقاس انتظار البحث والفهرسة كل منهما.

### [x] MU10-22 — P1 — عزل موارد OCR والتحقق من PDF

- **الدليل:** `processor_optimized.py:80` يحول جميع الصفحات إلى صور دفعة واحدة، و`:83` يشغل عدة threads، و`:46` يستدعي Tesseract دون timeout. Compose لا يحدد CPU أو memory limits للخدمات. `backend/api.py:139` يحلل صفحات PDF داخل عملية API؛ فحص الحجم يقع بعد تسلم multipart إلى UploadFile، ولا توجد طبقة ingress تحد إجمالي جسم الطلب في المستودع.
- **الأثر:** حد 25MB/100 صفحة لا يضمن حد ذاكرة الصور أو زمن تفسير PDF. عملية معالجة واحدة قد تستنزف السيرفر أو تشغل عمليات فرعية بعد فقد العامل.
- **المطلوب:** استخراج/OCR على دفعات صفحات وعمليات قابلة للإيقاف بمهل ومجموعات عمليات واضحة، وحدود CPU/RAM، والتحقق الثقيل في بيئة محدودة الموارد، وضبط حد الرفع في proxy وUI وAPI مع حصة إجمالية لكل مستخدم.
- **شرط القبول:** PDF ممسوح كبير ضمن الحد وملف بطيء المعالجة لا يسببان OOM أو تعطيل /jobs؛ timeout يحرر العامل والعمليات الفرعية، وتبقى المحادثات ضمن أهداف القبول.

### [ ] MU10-23 — P1 — نقل الأرشيف القديم وschema ونسخ احتياطي وتراجع

- **الدليل:** `backend/db.py:35` يستخدم create_all فقط بلا migrations. `/documents` لا يرى إلا PostgreSQL الجديد، بينما المحرك ما زال يستخدم اسم الفهرس القديم `knowledge_base_optimized_v2`. لا توجد آلية نقل المستندات القديمة إلى document/version records أو سيناريو rollback كما في الخطة §12.
- **الأثر:** عند التحويل قد يبدو الأرشيف فارغًا رغم وجود نصوص قديمة قابلة للبحث، ويختلف مصدر UI عن الاسترجاع. create_all لا يرقي أعمدة وقواعد schema مستقبلًا.
- **المطلوب:** migrations بإصدارات، وsnapshot/backup واستعادة مجربة، وbackfill للأرشيف القديم مع `original_available=false` عند فقد PDF، ونقل/alias وخطة تراجع تحفظ المهام الجديدة. إضافة سياسة احتفاظ وتنظيف uploads غير المستخدمة ومقاطع staging دون حذف المحتوى المشترك المستخدم.
- **شرط القبول:** ترقية نسخة اختبار بها مستندات قديمة مع أصول مفقودة تحافظ على البحث وتعرضها للجميع؛ مقارنة counts وعينة أسئلة؛ تجربة rollback واستعادة تحفظ كل مهمة مقبولة بعد التحويل.

### [x] MU10-24 — P2 — ضبط إعدادات النشر وإصدارات الخدمات وفحوص الصحة

- **الدليل:** كل الأدوار تستخدم Dockerfile ثقيلًا واحدًا وحزم requirements ذات حدود دنيا مفتوحة. `Dockerfile` يورث HEALTHCHECK على 8501؛ workers وdispatcher لا تستبدله في Compose رغم عدم تشغيل Streamlit داخلها. env الخاص بالعمال لا يمرر إعدادات OPENROUTER_TIMEOUT/MAX_TOKENS/RETRIES وغيرها الموجودة للواجهة. MAX_* مكتوبة كثوابت Compose، فلا تتأثر بالقيم المضافة إلى `.env.example` كما قد يتوقع المشغل. DATABASE_URL يدمج كلمة المرور مباشرة في URI.
- **المطلوب:** صور/اعتمادات مناسبة لكل دور وإصدارات قابلة لإعادة البناء، وفحوص worker/dispatcher ذات معنى، وتمرير الإعدادات المعلنة والتحقق منها، وبناء عنوان DB مع ترميز صحيح للمحارف الخاصة في كلمة المرور. توثيق readiness وترتيب التبعيات للبدء البارد.
- **شرط القبول:** تغيير حدود .env ينعكس على الخدمة الصحيحة؛ كلمة مرور بها `@` أو `/` لا تكسر الاتصال؛ لا يُبلغ worker سليم بأنه unhealthy بسبب منفذ UI؛ إعادة البناء تستخدم الإصدارات المختبرة نفسها.

### [x] MU10-25 — P2 — مراقبة السعة والطوابير وإحصاءات متسقة

- **الدليل:** Job يخزن heartbeat لكنه لا يعرضه في `job_payload()` ولا توجد مراقبة لعمر الطابور أو إنذار امتلاء Redis/قرص. `engine_optimized.py:101` يكتب JSON stats مباشرة من المحركات، وCompose يربط `/app/stats` للواجهة فقط؛ إحصاءات workers غير مجمعة أو مستديمة بهذا الربط. لا توجد قياسات p95/RSS/عدالة كما في الخطة §11.
- **المطلوب:** metrics للانتظار والمراحل والنتائج والموارد والـretries، وإنذارات قبل نفاد السعة، وتخزين إحصاءات متزامن مركزي أو backend metrics بدل ملف JSON مشترك غير محمي. إظهار وقت الانتظار والتقدم الحقيقي للمستخدم، وإيقاف polling السريع بعد الحالات النهائية.
- **شرط القبول:** لوحة/تقرير يحدد سبب التعليق بين queue/model/OCR/search/provider؛ فشل مزود لا يُحتسب نجاحًا؛ عدادات متطابقة عبر workers وإنذار تجريبي لقدم مهمة وامتلاء السعة.

### [ ] MU10-26 — P1 — اختبار قبول فعلي وتصحيح بيان الجاهزية

- **الدليل:** `test_platform_smoke.py:13` يرسل أسئلة ثم يفحص رسالة user فقط ولا ينتظر أي Job إلى terminal؛ لا يرفع PDF ولا يشغل OCR أو ردًا حقيقيًا. token/request_id ثابتان، ولذلك تشغيله ثانية قد يرجع Job محادثة سابقة ويبقي محادثة الاختبار الجديدة بلا رسالة. التقرير 014 يصف أجزاء من الاستعادة والإلغاء على أنها منفذة دون هذه الأدلة.
- **المطلوب:** suite قابلة للتكرار ومعزولة ببيانات جديدة، تشمل API/PostgreSQL والعمال والوسيط ومحرك البحث و10 متصفحات مستقلة. تنفيذ كل سيناريو §11: بداية عشرية، حمل مختلط، OCR، ملفات متشابهة الأسماء/المحتوى، refresh، قتل عامل، توقف/امتلاء broker، أخطاء المزود والتعافي. مزود اختباري مضبوط أولًا ثم OpenRouter حقيقي محدود؛ ساعة حمل بعد استقرار التنفيذ. تحديث تقرير الجاهزية بالمنجز المثبت والمتبقي.
- **شرط القبول:** تقرير بمواصفات بيئة الاختبار وإصداراتها؛ صفر مهام مقبولة ضائعة أو اختلاط نطاق/رسائل أو نشر جزئي، p95 قبول/حالة أقل من ثانية وانعكاس UI خلال 5 ثوانٍ، أقل من 1% أخطاء غير متوقعة بلا OOM/restart خلال الساعة، وبقية أهداف الزمن/الذاكرة المحددة بالخطة. لا يُستبدل اكتمال الرد والفهرسة بعدد HTTP 200.

## ترتيب التنفيذ والتبعيات

1. **إزالة الموانع:** MU10-01 وMU10-02 وMU10-03؛ تسجيل العمال وحماية الإدارة والهوية قبل تجربة مشتركة.
2. **عقود المهام والاتساق:** MU10-11 وMU10-12 وMU10-13 وMU10-16 وMU10-17، ثم MU10-14 وMU10-15 وMU10-18 وMU10-19؛ تُختبر معاملات PostgreSQL وحدود النشر هنا.
3. **تكامل الواجهة والأرشيف:** MU10-04 إلى MU10-10؛ بعد استقرار عقود API والهوية ومعرفات المستندات.
4. **الموارد والتشغيل والنقل:** MU10-20 إلى MU10-25؛ يبدأ إعداد fixtures ومراقبة الحمل بالتوازي مع إصلاح العقود، دون تشغيل حمل على بيانات الإنتاج.
5. **بوابة الاعتماد:** MU10-26 بعد إغلاق العيوب؛ يبدأ اختبار القبول تدريجيًا 2 ثم 5 ثم 10 مستخدمين.

**عدد التاسكات:** 26 — كلها مفتوحة. إضافة إعداد PostgreSQL وتشغيل `docker compose up` وحدهما لا يغلقان هذه القائمة ولا يثبتان جاهزية عشرة مستخدمين.
